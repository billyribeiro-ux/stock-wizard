# Base URL of the Python FastAPI backend. Read server-side only.
API_BASE_URL=http://localhost:8000

# Internal service token used to authenticate the SvelteKit server with FastAPI.
# This NEVER reaches the browser.
INTERNAL_API_TOKEN=dev-token
